export default function Footer() {
  return (
    <footer className="admin-footer">
      <span>© {new Date().getFullYear()} CRMPro — Built with Next.js &amp; React-Bootstrap</span>
      <span>v1.0.0</span>
    </footer>
  );
}
